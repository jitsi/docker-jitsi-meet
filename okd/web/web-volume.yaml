objects:
- <<: (( merge ))
- apiVersion: v1
  kind: PersistentVolumeClaim
  metadata:
    labels:
      app: jitsi
    name: web-letsencrypt-volume
  spec:
    accessModes:
    - ReadWriteOnce
    resources:
      requests:
        storage: 128Mi
